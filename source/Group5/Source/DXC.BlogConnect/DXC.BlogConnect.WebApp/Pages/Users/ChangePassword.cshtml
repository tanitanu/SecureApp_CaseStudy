@page
@model DXC.BlogConnect.WebApp.Pages.Users.ChangePasswordModel
@{
}
<h5>Edit Profile</h5>
<div class="row">
    <div class="col-md-4">

        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
          @*  @foreach (var item in Model.ErrorList)
            {
                <span asp-validation-for="@item.Description" class="text-danger">@item.Description</span>
            }*@
            
            <div class="form-group">
                <label  class="control-label">Old Password</label>
                <input asp-for="PasswordUser.Password" class="form-control" />
                <span asp-validation-for="PasswordUser.Password" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label class="control-label">New Password</label>
                <input asp-for="PasswordUser.Password" class="form-control" />
                <span asp-validation-for="PasswordUser.Password" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label  class="control-label">Confirm PAssword</label>
                <input asp-for="PasswordUser.Password" class="form-control" />
                <span asp-validation-for="PasswordUser.Password" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Update" class="btn btn-primary" />
            </div>
        </form>
    </div>

</div>
